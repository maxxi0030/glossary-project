# Glossary Project

**Glossary Project** — это веб-приложение для управления глоссарием терминов с дополнительным функционалом валидации SRT-файлов субтитров. Проект разработан на PHP с использованием чистого HTML/CSS без фреймворков, что демонстрирует глубокое понимание основ веб-разработки.

---

## Основной функционал

### 1. Управление глоссарием
- CRUD операции: Создание, чтение, редактирование и удаление терминов
- Импорт/экспорт: Загрузка CSV файлов и выгрузка в CSV формате
- Поиск: Фильтрация терминов по ключевым словам
- Сортировка: Упорядочивание терминов по алфавиту (A→Z, Z→A)
- Хранение данных: Использование JSON для персистентности данных

### 2. Валидация SRT субтитров
- Парсинг SRT: Обработка файлов субтитров
- Проверка временных меток: Валидация таймкодов
- Проверка текста: Контроль длины строк и количества строк
- Детальные отчеты: Ошибки и предупреждения с рекомендациями

---

## Архитектура проекта

```
glossary-project/
├── nn.php                    # Главный файл приложения (UI + маршрутизация)
├── logic.php                 # Бизнес-логика для глоссария
├── srt_logic.php            # Логика валидации SRT файлов
├── style2.css               # Стили приложения
├── glossary_data.json       # Хранилище данных
├── run_tests.php            # Запуск тестов
│
├── tests/                   # Модульные тесты
│   ├── test_add.php
│   ├── test_delete.php
│   ├── test_filter.php
│   ├── test_redact.php
│   └── test_sort.php
│
├── csv_files/               # Примеры CSV файлов
├── srt_files/               # Примеры SRT файлов
├── img/                     # Иконки интерфейса
└── chernoviki/              # Черновики и наброски
```

---

## Детальный анализ компонентов

### nn.php — Основной файл приложения

**Ответственность:**
- Обработка HTTP запросов (GET/POST)
- Рендеринг HTML интерфейса
- Управление сессиями и состоянием
- Координация между бизнес-логикой и представлением

**Ключевые особенности:**
1. **Разделение режимов работы**:
   - Обычный режим просмотра
   - Режим редактирования (`edit_id`)
   - Режим добавления (`add_mode`)

2. **Обработка загрузки файлов**:
   - CSV файлы для глоссария
   - SRT файлы для валидации
   - Защита от дублирования

3. **Миграция данных**:
   - Автоматическое добавление ID к старым записям
   - Поддержка обратной совместимости

4. **Экспорт данных**:
   - Генерация CSV без создания временных файлов
   - Очистка данных от переносов строк и кавычек
   - Использование `php://output` для прямой отправки в браузер


```

---

### logic.php — Бизнес-логика глоссария

Содержит чистые функции для работы с данными:

#### 1. filterGlossary() — Фильтрация терминов
```php
function filterGlossary($terms, $searchQuery)
```
**Эволюция:**
- **Версия 1.0**: Использование `stripos()` (только ASCII)
- **Версия 2.0**: Переход на `mb_stripos()` с поддержкой UTF-8 для корректной работы с кириллицей и латышскими диакритиками

**Особенности:**
- Поиск без учета регистра
- Поддержка многобайтовых символов
- Возвращает все совпадения

#### 2. sortGlossary() — Сортировка терминов
```php
function sortGlossary($terms, $sortOrder)
```
**Эволюция:**
- **Версия 1.0**: `sort()` + `array_column()` — простая сортировка
- **Версия 2.0**: `usort()` + `strcasecmp()` — более эффективный подход
- **Версия 3.0**: Кастомная сортировка с весами символов

**Проблема, которую решает версия 3.0:**
Латышские диакритические буквы (ā, č, ē, ģ, ī, ķ, ļ, ņ, š, ū, ž) и кириллица требуют специального порядка сортировки:
```php
$alphabet = [
    'Ā' => 0.5,  'ā' => 0.51,
    'A'  => 1.0, 'a'  => 1.01,
    // ... диакритики идут ПЕРЕД базовой буквой
];
```

**Алгоритм:**
1. Нормализация строк (NFC)
2. Преобразование символов в веса
3. Лексикографическое сравнение массивов весов
4. Fallback для неизвестных символов через Unicode codepoint

#### 3. addTerm() — Добавление термина
```php
function addTerm($terms, $newTerm, $newTranslation)
```
**Особенности:**
- Автоинкремент ID
- Валидация пустых полей
- Добавление в начало массива (`array_unshift`)
- Возврат структурированного результата

#### 4. readactTerm() — Редактирование термина
```php
function readactTerm($terms, $editId, $editedTerm, $editedTranslation)
```
**Безопасность:**
- Проверка существования термина
- Использование `unset($term)` после цикла foreach для очистки ссылок

#### 5. deleteTerm() — Удаление термина
```php
function deleteTerm($terms, $deleteId)
```
**Особенности:**
- Типизация ID: `(int)$deleteId`
- Использование `array_filter()` для удаления
- `array_values()` для переиндексации массива

---

### srt_logic.php — Логика валидации SRT

Модуль для парсинга и валидации файлов субтитров.

#### parseSRT() — Парсинг SRT файла
```php
function parseSRT($SRTfileContent)
```
**Алгоритм:**
1. Нормализация переносов строк (`\r\n` → `\n`)
2. Разбиение на блоки по пустым строкам
3. Извлечение для каждого блока:
   - Индекс субтитра
   - Временные метки (startTime, endTime)
   - Текст (может быть многострочным)

**Структура вывода:**
```php
[
    'index' => 1,
    'startTime' => '00:00:10,500',
    'endTime' => '00:00:15,000',
    'text' => 'Subtitle text line 1\nSubtitle text line 2'
]
```

#### toMiliseconds() — Конвертация таймкодов
```php
function toMiliseconds($time)
```
**Формат:** `00:00:10,500` → `10500ms`

**Формула:**
```
ms = (h × 3,600,000) + (m × 60,000) + (s × 1,000) + ms
```

#### Функции валидации

##### 1. endEarlier() — ERROR: NEGATIVE_DURATION
Проверяет, что конец субтитра позже начала:
```php
if($start >= $end) { /* ERROR */ }
```

##### 2. crossWprevious() — ERROR: OVERLAP
Проверяет пересечение с предыдущим сегментом:
```php
if ($this_start < $prev_end) { /* ERROR */ }
```
Вычисляет величину пересечения: `$overlap = $prev_end - $this_start`

##### 3. minGap() — WARN: GAP_TOO_SMALL
Минимальный промежуток между субтитрами должен быть ≥ 160ms:
```php
if($gap < 160 && $gap >= 0) { /* WARNING */ }
```

##### 4. durationTooShort() — WARN: DURATION_TOO_SHORT
Субтитр слишком короткий (< 1080ms):
```php
if($duration < 1080 && $duration >= 0) { /* WARNING */ }
```

##### 5. durationTooLong() — WARN: DURATION_TOO_LONG
Субтитр слишком длинный (> 7000ms):
```php
if($duration > 7000) { /* WARNING */ }
```

##### 6. emptyText() — ERROR: EMPTY_TEXT
Проверяет наличие видимого текста:
```php
if(empty(trim($text))) { /* ERROR */ }
```

##### 7. tooManyLines() — WARN: TOO_MANY_LINES
Максимум 2 строки в одном субтитре:
```php
if (count(explode("\n", $text)) > 2) { /* WARNING */ }
```

##### 8. tooLongLine() — WARN: LINE_TOO_LONG
Длина строки ≤ 36 символов:
```php
$symbols = grapheme_strlen($line); // Правильный подсчет с эмодзи
if ($symbols > 36) { /* WARNING */ }
```

**Эволюция подсчета символов:**
- `mb_strlen()` — плохо работает с эмодзи
- `strlen()` — только для ASCII
- `grapheme_strlen()` — правильный подсчет графем (включая эмодзи)

#### checkAll() — Сборка всех проверок
```php
function checkAll($subtitles)
```
Возвращает структуру:
```php
[
    'errors' => [...],    // Критичные ошибки
    'warnings' => [...]   // Предупреждения
]
```

---

## Тестирование

Проект содержит набор модульных тестов в папке `tests/`:

### Структура тестов:

1. **test_add.php** — Тестирование добавления терминов
   - Добавление валидных данных
   - Обработка пустых полей
   - Проверка автоинкремента ID

2. **test_delete.php** — Тестирование удаления
   - Удаление существующих терминов
   - Обработка несуществующих ID
   - Переиндексация массива

3. **test_filter.php** — Тестирование фильтрации
   - Поиск по частичному совпадению
   - Поиск с кириллицей и диакритиками
   - Пустой запрос возвращает все термины

4. **test_redact.php** — Тестирование редактирования
   - Изменение существующих терминов
   - Валидация пустых полей
   - Проверка на несуществующие ID

5. **test_sort.php** — Тестирование сортировки
   - Сортировка A→Z
   - Сортировка Z→A
   - Корректная работа с латышскими диакритиками
   - Корректная работа с кириллицей

### Запуск тестов:
```bash
php run_tests.php
```

---

## Технические решения и их обоснование

### 1. Хранение данных в JSON вместо БД

**Преимущества:**
- Простота развертывания (не требуется MySQL)
- Легкость бэкапа (один файл)
- Читаемость данных
- Портативность

**Недостатки:**
- Не масштабируется для больших объемов
- Нет транзакций
- Конкурентный доступ может привести к race conditions

**Вывод:** Оптимально для учебного проекта и небольших глоссариев (< 1000 терминов)

### 2. Обработка файлов через POST без AJAX

**Преимущества:**
- Работает без JavaScript
- Простая отладка
- Надежная отправка файлов

**Недостатки:**
- Перезагрузка страницы
- Нет прогресс-бара загрузки

**Решение:** Использование `header('Location: ...')` для предотвращения повторной отправки при F5

### 3. Кастомная сортировка вместо Collator

**Причина:**
`Collator` из PHP Intl extension не всегда доступен на shared hosting.

**Решение:**
Собственная реализация с картой весов символов:
```php
$alphabet = [
    'Ā' => 0.5, 'A' => 1.0,  // Диакритики перед базовой буквой
    // ...
];
```

**Преимущества:**
- Полный контроль над порядком сортировки
- Не зависит от установленных расширений PHP
- Поддержка смешанных алфавитов (латиница + кириллица)

### 4. Использование grapheme_strlen() для подсчета символов

**Проблема:**
```php
mb_strlen('👨‍👩‍👧‍👦')  // = 7 (неправильно)
grapheme_strlen('👨‍👩‍👧‍👦')  // = 1 (правильно)
```

**Решение:**
Использование `grapheme_strlen()` для корректного подсчета графемных кластеров (эмодзи, комбинированные символы).

---

## Пользовательский интерфейс

### Особенности UI:

1. **Адаптивная таблица**
   - Четкое разделение колонок
   - Иконки для действий (редактировать, удалить)
   - Inline редактирование с `<textarea>`

2. **Управление состоянием через GET параметры**
   ```
   ?edit_id=5       # Режим редактирования
   ?add_mode=1      # Режим добавления
   ?sort=asc        # Сортировка
   ?search=term     # Поиск
   ```

3. **Визуальная обратная связь**
   - Ошибки и предупреждения SRT в разных секциях
   - Цветовое кодирование (ERROR — красный, WARNING — желтый)
   - Советы по исправлению

4. **Счетчики**
   - Общее количество терминов
   - Количество отфильтрованных терминов

---

## Анализ кода

### Сильные стороны:

1. **Модульность**
   - Четкое разделение логики (`logic.php`) и представления (`nn.php`)
   - Переиспользуемые функции
   - Легко тестируемый код

2. **Эволюция кода**
   - Видны итерации улучшений (комментарии со старыми версиями)
   - Постепенное решение проблем с Unicode
   - Рефакторинг для эффективности

3. **Обработка ошибок**
   - Валидация входных данных
   - Проверка существования записей
   - Структурированные ответы функций

4. **Документация**
   - Подробные комментарии на русском
   - Объяснение нестандартных решений
   - Пометки TODO и проблемных мест

### Области для улучшения:

1. **Безопасность**
   - Нет защиты от CSRF атак
   - Отсутствует санитизация при экспорте CSV
   - Нет ограничения размера загружаемых файлов

   **Рекомендации:**
   ```php
   // CSRF token
   session_start();
   $_SESSION['csrf_token'] = bin2hex(random_bytes(32));
   
   // Проверка размера файла
   if ($_FILES['file']['size'] > 5 * 1024 * 1024) { /* ERROR */ }
   ```

2. **Валидация**
   - Нет проверки на дублирующиеся термины при добавлении
   - Отсутствует ограничение длины текста

   **Рекомендации:**
   ```php
   function isDuplicateTerm($newTerm, $terms) {
       foreach ($terms as $term) {
           if (mb_strtolower($term['term']) === mb_strtolower($newTerm)) {
               return true;
           }
       }
       return false;
   }
   ```

3. **Производительность**
   - Повторное чтение/запись JSON при каждой операции
   - Нет кеширования отсортированных данных

   **Рекомендации:**
   - Использовать кеш для часто запрашиваемых данных
   - Батчинг операций записи

4. **Масштабируемость**
   - JSON файл не подходит для > 10,000 записей
   - Нет пагинации

   **Рекомендации:**
   - Миграция на SQLite для средних объемов
   - Добавление пагинации (по 50 записей на страницу)

---

## История развития проекта

Согласно commit history:

1. **15 декабря 2025** — Initial commit
   - Базовая структура проекта
   - Первая версия глоссария

2. **16 декабря 2025** — Работа над SRT логикой
   - Добавление модуля валидации субтитров
   - Интеграция в основное приложение

3. **17 декабря 2025** — Масштабный рефакторинг
   - Улучшение парсинга SRT
   - Обработка отсутствующего текста субтитров
   - Улучшение сообщений об ошибках
   - Добавление примеров CSV и SRT файлов
   - Создание тестов

4. **11 февраля 2026** — Чистка репозитория
   - Удаление системных файлов (.DS_Store, .vscode)
   - Подготовка к публикации

---

## Как запустить проект

### Требования:
- PHP 7.4+
- Расширения: `json`, `mbstring`, `intl` (опционально)
- Веб-сервер (Apache/Nginx) или встроенный сервер PHP

### Локальный запуск:

1. Клонируйте репозиторий:
```bash
git clone https://github.com/maxxi0030/glossary-project.git
cd glossary-project
```

2. Запустите встроенный сервер PHP:
```bash
php -S localhost:8000
```

3. Откройте в браузере:
```
http://localhost:8000/nn.php
```

### Использование:

1. **Импорт глоссария:**
   - Подготовьте CSV файл: `термин,перевод`
   - Нажмите "Import" → выберите файл

2. **Работа с терминами:**
   - Добавить: кнопка "+" в шапке таблицы
   - Редактировать: иконка карандаша
   - Удалить: иконка корзины
   - Поиск: введите запрос в поле Search
   - Сортировка: кликните A→Z или Z→A

3. **Валидация SRT:**
   - Загрузите .srt файл через секцию "Import SRT"
   - Получите детальный отчет с ошибками и рекомендациями

4. **Экспорт:**
   - Нажмите "Download" для сохранения глоссария в CSV

---

## Метрики проекта

### Размер кодовой базы:
- **nn.php**: ~30KB (главный файл)
- **logic.php**: ~11KB (бизнес-логика)
- **srt_logic.php**: ~12KB (валидация SRT)
- **style2.css**: ~13KB (стили)
- **Тесты**: ~10KB (5 файлов)

**Итого:** ~76KB кода (без зависимостей)

### Покрытие функциями:
- Управление глоссарием: 5 функций
- Валидация SRT: 9 функций
- Вспомогательные: 3 функции

### Поддерживаемые форматы:
- **Ввод:** CSV, SRT
- **Вывод:** CSV, JSON (внутри)
- **Кодировка:** UTF-8


---

## Выводы

### Что делалось:

1. **Глоссарий-менеджер** с полным CRUD функционалом
2. **Валидатор SRT-субтитров** с детальной диагностикой
3. **Импорт/экспорт** в CSV формате
4. **Кастомная сортировка** для латышского и кириллицы
5. **Набор модульных тестов** для проверки корректности

### Как делалось:

1. **Итеративная разработка:**
   - Начало с простых функций (v1.0)
   - Постепенное улучшение (v2.0, v3.0)
   - Рефакторинг для читаемости и эффективности

2. **Решение реальных проблем:**
   - Unicode в сортировке → кастомные веса
   - Эмодзи в субтитрах → `grapheme_strlen()`
   - Переносы в CSV → очистка и экранирование

3. **Тестирование:**
   - Создание unit-тестов для каждой функции
   - Проверка граничных случаев
   - Валидация с реальными данными

### Как анализировалось:

1. **Структурный анализ:**
   - Разбор архитектуры файлов
   - Оценка модульности
   - Проверка разделения ответственности

2. **Функциональный анализ:**
   - Трассировка логики каждой функции
   - Выявление улучшений между версиями
   - Оценка эффективности алгоритмов

3. **Анализ качества:**
   - Читаемость кода
   - Наличие документации
   - Обработка ошибок

---

## Технический стек

- **Backend:** PHP 7.4+
- **Frontend:** HTML5, CSS3
- **Data Storage:** JSON
- **Testing:** Custom PHP unit tests
- **Version Control:** Git
